- name: using variable from command line
  hosts: localhost
  tasks:
  - name: printing variable values at command line
    ansible.builtin.debug:
      msg: "hii iam {{PERSON}}, learning {{TOOL}}"

# giveing varible values from command line arguments -e "variable_name=value"
# -e "PERSON=HARI KRISHNA" -e "TOOL=AWS"
